<div class="list-group">
	{% for item in categories %}
		<a href="{{ url('app_category', {slug: item.slug}) }}" class="list-group-item list-group-item-action">
			{{ item.name | upper }}
		</a>
	{% endfor %}
</div>
